style:
  WildcardImport:
    active: false

processors:
  active: true
  exclude:
    - 'ProjectCLOCProcessor'
    - 'DetektProgressListener'
    - 'KtFileCountProcessor'
    - 'PackageCountProcessor'
    - 'ClassCountProcessor'
    - 'FunctionCountProcessor'
    - 'PropertyCountProcessor'
  # - 'ProjectComplexityProcessor'
  # - 'ProjectCognitiveComplexityProcessor'
  # - 'ProjectLLOCProcessor'
  # - 'ProjectLOCProcessor'
  # - 'ProjectSLOCProcessor'
  # - 'LicenseHeaderLoaderExtension'

package-restriction:
  PackageRestrictionRule:
    active: true
    project-package-prefix: io.ytka.packagerestriction  # prefixにマッチするパッケージ名を対象とする。import先のチェックもこのprefixを含むものだけを対象とする。
    packages:
      - name: io.ytka.packagerestriction.detektext
        import-allows:
          - io.ytka.packagerestriction.import
        cyclomatic-complexity-threshold: 3
      - name: io.ytka.packagerestriction.import
        import-denys:
          - "*" # 対象パッケージからのimportをチェックし、import先がこの条件にマッチする場合、違反とする。package-prefixをプレフィックスとして補完した上でマッチングする。
        cyclomatic-complexity-threshold: 10
